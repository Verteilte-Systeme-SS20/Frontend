---
applications:
  - name: frontend
    memory: 768M
    host: fontend-service
    path: target/vs_frontend-0.0.1-SNAPSHOT.jar
    env:
      spring.profiles.active: cloud
    services:
      - eureka_server